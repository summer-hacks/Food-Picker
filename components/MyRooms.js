import React from 'react';
import { Button, StyleSheet, Text, View, Dimensions, Image, Animated, PanResponder } from 'react-native';

function MyRooms() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>My Rooms</Text>
      </View>
    );
  }

export default MyRooms;